[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "python311", "gcc-unwrapped", "gnumake"]

[phases.install]
cmds = ["npm install --legacy-peer-deps --include=dev"]

[phases.build]
cmds = ["npm rebuild better-sqlite3"]

[start]
cmd = "npx tsx apps/api/src/index.ts"
